<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="mx.easycode.coachapp.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_miRendimientoFragment"
            app:destination="@id/miRendimientoFragment" />
    </fragment>
    <fragment
        android:id="@+id/miRendimientoFragment"
        android:name="mx.easycode.coachapp.MiRendimientoFragment"
        android:label="fragment_mi_rendimiento"
        tools:layout="@layout/fragment_mi_rendimiento" />
</navigation>